// import React from 'react';

export const Pagination = ({video}) => {
    console.log()
    // const totalPages = (videos.pageInfo.totalResults) / 5
    // console.log (totalPages)
    return (
        123
    )
}